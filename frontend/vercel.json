{
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "https://brunette-backend.onrender.com/:path*"
        }
    ],
    "build": {
        "env": {
            "NPM_FLAGS": "--legacy-peer-deps"
        }
    }
}