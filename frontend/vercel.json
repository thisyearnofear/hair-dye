{
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "https://brunette-backend.onrender.com/api/:path*"
        }
    ],
    "build": {
        "env": {
            "NPM_FLAGS": "--legacy-peer-deps"
        }
    }
}